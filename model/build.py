"""Placeholder for the upcoming model reimplementation."""


class PASModel:
    """Temporary placeholder class for the future PAS model implementation."""

    def __init__(self, *args, **kwargs):
        raise NotImplementedError(
            "PASModel is currently a placeholder. "
            "The legacy model implementation was intentionally excluded and "
            "will be reimplemented."
        )


def build_model(*args, **kwargs):
    """Temporary placeholder factory for the future model implementation."""

    raise NotImplementedError(
        "build_model is currently a placeholder. "
        "The legacy model implementation was intentionally excluded and "
        "will be reimplemented."
    )

